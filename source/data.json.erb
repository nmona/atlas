<%= JSON.pretty_generate(data.networks.map{ |id, network|
  network.merge({
    id: id,
    generated: {
      image_path: 'http://atlas.networkedartifacts.info' + image_path("networks/#{network.meta.image}"),
      explanation: strip_tags(explain(network))
    }
  })
}) %>
