<%= JSON.pretty_generate(data.networks.map{ |id, network|
  network.merge({
    id: id,
    generated: {
      illustration: image_path("networks/#{network.meta.illustration}"),
      explanation: explain(network)
    }
  })
}) %>
